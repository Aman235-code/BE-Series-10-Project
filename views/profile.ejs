<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex items-end justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 overflow-hidden rounded-md">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profile %>" alt="">
            </div>
            <h3 class="text-3xl mb-5"><span class="font-light">Hello</span>, <%= user.name %>
            </h3>
        </div>

        <h5 class="mb-5 mt-3">You can create a new post.</h5>
        <form method="post" action="/post">
            <textarea name="content" id="" placeholder="What's on your mind?"
                class="p-3 outline-none w-1/3 resize-none block bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input type="submit" value="Create New Post"
                class="text-sm px-3 py-2 mt-2 rounded-md w-40 bg-blue-500 block">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="post-container mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">
                            @<%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tighter">
                            <%= post.content %>
                        </p>
                        <small class="mt-2 inline-block">
                            <%= post.likes.length %> likes
                        </small>
                        <div class="btns flex mt-5 gap-4">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                    <% }) %>
            </div>
        </div>


    </div>
</body>

</html>